{
  "name": "cine-journal-build",
  "private": true,
  "description": "One-off Tailwind compile for CinéJournal. The shipped site in ../ needs no build step — this only regenerates ../css/app.css.",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i input.css -o ../css/app.css --minify",
    "dev": "tailwindcss -c tailwind.config.js -i input.css -o ../css/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/container-queries": "^0.1.1"
  }
}
